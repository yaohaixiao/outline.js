/**
 * 检测数据是否为 Number 类型
 * ========================================================================
 * @method isNumber
 * @param {*} o
 * @returns {boolean}
 */
const isNumber = (o) => {
  return typeof o === 'number'
}

export default isNumber
