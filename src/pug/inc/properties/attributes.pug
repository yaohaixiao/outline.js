div#property-attributes.api-md
    div.api-hd
        h3.api-title attributes
    div.api-bd
        h4 Description
        dl.api-param
            dt.api-param-key Type：
            dd.api-param-value Object
        p 存储的是 ArticleOutline 对象当前使用中的配置选项：
        p 说明：建议使用 get(prop) 方法来获取属性，避免直接调用属性。