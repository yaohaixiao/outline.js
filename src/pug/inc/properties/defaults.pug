div#property-defaults.api-md
    div.api-hd
        h3.api-title defaults
    div.api-bd
        h4 Description
        dl.api-param
            dt.api-param-key Type：
            dd.api-param-value Object
        p 静态属性，存储的是 ArticleOutline 对象默认配置选项：
        dl.api-param
            dt.api-param-key article：
            dd.api-param-value 用来指定页面中显示文章正文的 DOM 节点的 ID 选择器。（默认值：'#article'）
        dl.api-param
            dt.api-param-key selector：
            dd.api-param-value 用来指定 article 节点下，要生成导航的标题标签的选择器。（默认值：'h1,h2,h3,h4,h5,h6'）
        dl.api-param
            dt.api-param-key title：
            dd.api-param-value 用来指定文章导读导航菜单的标题文字。（默认值：'文章导读'）
        dl.api-param
            dt.api-param-key position：
            dd.api-param-value 用来指定文章导读导航菜单的显示位置。（默认值：'outside'）
        dl.api-param
            dt.api-param-key anchorURL：
            dd.api-param-value 用来指定文章标题锚点链接图标的链接地址。（默认值：''）
        dl.api-param
            dt.api-param-key anchorAt：
            dd.api-param-value 用来指定文章标题锚点链接图标的显示位置。（默认值：'front'）
        dl.api-param
            dt.api-param-key isGenerateOutline：
            dd.api-param-value 用来指定是否文章导读导航菜单。（默认值：true）
        dl.api-param
            dt.api-param-key isGenerateOutlineChapterCode：
            dd.api-param-value 用来指定是否文章导读导航菜单是否显示文章段落层次的索引编号。（默认值：true）
        dl.api-param
            dt.api-param-key isGenerateHeadingChapterCode：
            dd.api-param-value 用来指定是否在文章标题前面显示文章段落层次的索引编号。（默认值：false）
        dl.api-param
            dt.api-param-key isGenerateHeadingAnchor：
            dd.api-param-value 用来指定是否在文章标题位置生成锚点链接图标。（默认值：true）