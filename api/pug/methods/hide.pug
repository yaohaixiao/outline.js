section.section
  header.section__header
    h3#method-hide.section__h3 hide()
  div.section__content
    h4.section__h4 Description
    p hide() 方法用来收起侧边栏菜单。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Outline
    p Outline 对象。
    pre.section__pre
      code.section__code.
        const outline = new Outline({
             article: '#article',
             title: 'Outline v2.0.0'
        })

        outline.show()
        outline.hide() // -> 导航菜单将收起
