section.section
  header.section__header
    h3.section__h3 closeOnClickModal
  div.section__content
    h4.section__h4 Description
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean
    dl.section__dl
      dt.section__dt Default：
      dd.section__dd true
    p 可选，点击空白处（非独立导航菜单），收起导航菜单：
    ul
      li true - 点击空白处，收起菜单（默认值）；
      li false - 点击空白处，不收起菜单；
