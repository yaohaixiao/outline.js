section.section
  header.section__header
    h3.section__h3 created
  div.section__content
    table.section__table
      thead.section__thead
        tr.section__tr
          th.section__th 事件名称
          th.section__th 说明
          th.section__th 回调参数
      tbody.section__tbody
        tr.section__tr
          td.section__td created
          td.section__td Outline 实例配置信息初始化完成后触发，此时 DOM 元素还没有绘制。
          td.section__td 返回实例最终的 attrs 配置属性的（对象）值。
    h4.section__h4 Usage
    pre.section__pre
      code.section__code.
        const outline = new Outline()

        outline.$on('created', function(attrs) {
          // this 上下文就是 outline 实例
          // attrs === outline.attr()
        })

        // 当 outline 实例的配置信息初始化完毕，就会触发 created 事件
        outline.initialize(Outline.DEFAULTS)
